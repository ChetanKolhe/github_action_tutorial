name: My first workflow 
on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repository
              uses: Actions/Checkout@main

            - name: welcome message 
              run: |
                echo "My first Github Actions"
                ls
                cat README.md

            - name: Generate ASCII char dragon
              run: cowsy -f dragon "AWW .. I Am Dragon" > dragon.txt
              
            # - name : View the file 
            #   run : cat README.md